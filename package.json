{
  "name": "my-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "res:build": "rescript",
    "res:clean": "rescript clean",
    "res:dev": "rescript watch",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "rescript && vitest run",
    "test:watch": "vitest",
    "res:analyze": "rescript && rescript-tools reanalyze -dce",
    "res:analyze:json": "rescript && rescript-tools reanalyze -dce -json",
    "check": "rescript && rescript-tools reanalyze -dce -json && eslint src/ && vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@rescript/react": "^0.14.0",
    "@rescript/runtime": "12.2.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "rescript": "12.2.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.12",
    "@vitejs/plugin-react": "^5.0.2",
    "eslint": "^10.0.2",
    "eslint-plugin-react-hooks": "^7.0.1",
    "rescript-vitest": "^2.1.1",
    "tailwindcss": "^4.1.12",
    "vite": "^7.1.0",
    "vitest": "^3.2.4"
  }
}
